{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h2>Promotion</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Promotion</th>
                <td>{{ promotion.promotion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">Éditer</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
