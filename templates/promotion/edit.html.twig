{% extends 'base.html.twig' %}

{% block title %}Éditer Promotion{% endblock %}

{% block body %}
    <h2>Éditer Promotion</h2>

    {{ include('promotion/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('app_promotion_index') }}">Retour à la liste</a>

    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
