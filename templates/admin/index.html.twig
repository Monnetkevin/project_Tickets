{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}

<div class="admin">
    <div class="ticket">
        <h2 class="cardTickets__title">Promotion</h2>
        {% for promotion in promotions %}
        <div class="cardAccount">
            <h5>{{promotion.promotion}}</h5>
            <div class="cardAccount__content">
                <a href="{{ path('app_promotion_edit', {'id': promotion.id}) }}">Modifier</a>
            </div>
        </div>
        {% endfor %}
    </div>

    <div class="pageContainer">
        <h2>Utilisateur</h2>
        {% for user in users %}
        <div class="cardAccount">
            <h3>{{user.firstName}} {{user.lastName}}</h3>
            <h4>{% if (user.promotion) %}{{user.promotion.promotion}}{% else %}Aucune promotion affectée{% endif %}</h4>
            <div>
                <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
            </div>
        </div>
        {% endfor %}
    </div>
    
    <h2>Tickets</h2>
        <table class="visibiliyDesktop">
            <thead>
                <tr>
                    <th>De</th>
                    <th>Titre</th>
                    <th>Description</th>
                    <th>Date de Création</th>
                    <th>Statut</th>
                    <th>Dernier changement</th>
                    <th>Par</th>
                </tr>
            </thead>
            <tbody>
                {% for ticket in tickets %}
                <tr>
                    <td>{{ ticket.owner.firstName }} {{ ticket.owner.lastName }}</td>
                    <td>{{ ticket.title }}</td>
                    <td>{{ ticket.description|u.truncate(50, '...') }}</td>
                    <td>{{ ticket.dateCreate ? ticket.dateCreate|date('d/m/Y à H:i:s') : '' }}</td>
                    <td>{{ ticket.status.status }}</td>
                    <td>
                        {% if (ticket.lastUpdate) %}
                            {{ ticket.lastUpdate ? ticket.lastUpdate|date('d/m/Y à H:i:s') : '' }}
                        {% endif %}
                    </td>
                    <td> 
                        {% if (ticket.helper) %}
                            {{ ticket.helper.firstName }} {{ ticket.helper.lastName }}
                        {% endif %}
                    </td>
                    <td>
                        <div><a href="{{ path('app_ticket_show', {'id': ticket.id}) }}" class="">Voir</a></div>
                        <div><a href="{{ path('app_ticket_edit', {'id': ticket.id}) }}" class="">Modifier</a></div>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">Aucune information</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
            {% for ticket in tickets %}
                <table class="visibiliyMobile">
                    <tbody>
                        <tr>
                            <th>De</th>
                            <td>{{ ticket.owner.firstName }} {{ ticket.owner.lastName }}</td>
                        </tr>

                        <tr>
                            <th>Titre</th>
                            <td>{{ ticket.title }}</td>
                        </tr>

                        <tr>
                            <th>Description</th>
                            <td>{{ ticket.description|u.truncate(50, '...') }}</td>
                        </tr>

                        <tr>
                            <th>Date de Création</th>
                            <td>{{ ticket.dateCreate ? ticket.dateCreate|date('d/m/Y à H:i:s') : '' }}</td>
                        </tr>

                        <tr>
                            <th>Statut</th>
                            <td>{{ ticket.status.status }}</td>
                        </tr>

                        <tr>
                            <th>Dernier changement</th>
                            <td>
                                {% if (ticket.lastUpdate) %}
                                    {{ ticket.lastUpdate ? ticket.lastUpdate|date('d/m/Y à H:i:s') : '' }}
                                {% endif %}
                            </td>
                        </tr>

                        <tr>
                            <th>Par</th>
                            <td> 
                                {% if (ticket.helper) %}
                                    {{ ticket.helper.firstName }} {{ ticket.helper.lastName }}
                                {% endif %}
                            </td>
                        </tr>

                        <tr>
                            <th></th>
                            <td>
                                <div><a href="{{ path('app_ticket_show', {'id': ticket.id}) }}" class="">Voir</a></div>
                                <div><a href="{{ path('app_ticket_edit', {'id': ticket.id}) }}" class="">Modifier</a></div>
                            </td>
                        </tr>
                        <tr>
                            <th style="background-color: transparent;"></th>
                            <td style="background-color: transparent;"></td>
                        </tr>

                    </tbody>
                </table>
            {% endfor %}
</div>





{% endblock %}
